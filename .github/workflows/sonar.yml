name: Run Sonar

on:
  workflow_dispatch:
  schedule:
    - cron: "0 15 * * SUN"   # 16:00 CET
    - cron: "0 14 * * SUN"   # 16:00 CEST


jobs:
  sonar:
    uses: onecx/ci-angular/.github/workflows/angular-sonar-main.yml@v1
    secrets: inherit
